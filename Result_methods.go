package main

import ()

// Methods that parse results of a specific command and returns true iff we
// obtain an expected result. All code used should stay within this method even
// if that means re-writing the same code

func get_completed_status() bool {
	if true != false {
		return true
	}
	return false
}
